openapi: "3.0.3"
info:
  title: "Generated Application API"
  version: "1.0.0"
  description: |
    API generated by RNC from legacy source code.
    Business rules are preserved from the original system.
servers:
  - url: http://localhost:3059/api
    description: Local development

paths:
  /nccruises:
    get:
      summary: List Nccruises
      operationId: listNccruises
      tags: [Nccruise]
      parameters:
        - name: page
          in: query
          schema: {type: integer, default: 1}
        - name: limit
          in: query
          schema: {type: integer, default: 20, maximum: 100}
      responses:
        "200":
          description: Paginated list of Nccruises
          content:
            application/json:
              schema: {$ref: "#/components/schemas/NccruiseList"}
    post:
      summary: Create Nccruise
      operationId: createNccruise
      tags: [Nccruise]
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/NccruiseInput"}
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema: {$ref: "#/components/schemas/Nccruise"}
        "400":
          description: Validation error
          content:
            application/json:
              schema: {$ref: "#/components/schemas/ValidationError"}
  "/nccruises/{id}":
    parameters:
      - name: id
        in: path
        required: true
        schema: {type: integer}
    get:
      summary: Get Nccruise by id
      operationId: getNccruiseById
      tags: [Nccruise]
      responses:
        "200":
          content:
            application/json:
              schema: {$ref: "#/components/schemas/Nccruise"}
        "404": {description: Not found}
    put:
      summary: Update Nccruise
      operationId: updateNccruise
      tags: [Nccruise]
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/NccruiseInput"}
      responses:
        "200":
          content:
            application/json:
              schema: {$ref: "#/components/schemas/Nccruise"}
        "400": {description: Validation error}
        "404": {description: Not found}
    delete:
      summary: Delete Nccruise
      operationId: deleteNccruise
      tags: [Nccruise]
      responses:
        "204": {description: Deleted}
        "404": {description: Not found}
  /ncyachts:
    get:
      summary: List Ncyachts
      operationId: listNcyachts
      tags: [Ncyacht]
      parameters:
        - name: page
          in: query
          schema: {type: integer, default: 1}
        - name: limit
          in: query
          schema: {type: integer, default: 20, maximum: 100}
      responses:
        "200":
          description: Paginated list of Ncyachts
          content:
            application/json:
              schema: {$ref: "#/components/schemas/NcyachtList"}
    post:
      summary: Create Ncyacht
      operationId: createNcyacht
      tags: [Ncyacht]
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/NcyachtInput"}
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema: {$ref: "#/components/schemas/Ncyacht"}
        "400":
          description: Validation error
          content:
            application/json:
              schema: {$ref: "#/components/schemas/ValidationError"}
  "/ncyachts/{id}":
    parameters:
      - name: id
        in: path
        required: true
        schema: {type: integer}
    get:
      summary: Get Ncyacht by id
      operationId: getNcyachtById
      tags: [Ncyacht]
      responses:
        "200":
          content:
            application/json:
              schema: {$ref: "#/components/schemas/Ncyacht"}
        "404": {description: Not found}
    put:
      summary: Update Ncyacht
      operationId: updateNcyacht
      tags: [Ncyacht]
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/NcyachtInput"}
      responses:
        "200":
          content:
            application/json:
              schema: {$ref: "#/components/schemas/Ncyacht"}
        "400": {description: Validation error}
        "404": {description: Not found}
    delete:
      summary: Delete Ncyacht
      operationId: deleteNcyacht
      tags: [Ncyacht]
      responses:
        "204": {description: Deleted}
        "404": {description: Not found}

components:
  schemas:
    Nccruise:
      type: object
      properties:
        id:
          type: integer
          readOnly: true
        cruiseStatus:
          type: string
          description: "Cruise Status"
        startDate:
          type: string
          nullable: true
          description: "Start Date"
        startTime:
          type: number
          description: "Start Time"
        endDate:
          type: number
          description: "End Date"
        endTime:
          type: number
          description: "End Time"
        startHarbor:
          type: string
          nullable: true
          description: "Start Harbor"
        destinationHarbor:
          type: string
          nullable: true
          description: "Destination Harbor"
        idYacht:
          type: number
          description: "Id Yacht"
        price1W:
          type: number
          description: "Price 1 W"
        price2W:
          type: number
          description: "Price 2 W"
        price3W:
          type: number
          description: "Price 3 W"
        createdAt:
          type: string
          format: date-time
          readOnly: true
        updatedAt:
          type: string
          format: date-time
          readOnly: true
    NccruiseInput:
      type: object
      required: ["cruiseStatus", "startTime", "endDate", "endTime", "idYacht", "price1W", "price2W", "price3W"]
      properties:
        cruiseStatus:
          type: string
          description: "Cruise Status"
        startDate:
          type: string
          nullable: true
          description: "Start Date"
        startTime:
          type: number
          description: "Start Time"
        endDate:
          type: number
          description: "End Date"
        endTime:
          type: number
          description: "End Time"
        startHarbor:
          type: string
          nullable: true
          description: "Start Harbor"
        destinationHarbor:
          type: string
          nullable: true
          description: "Destination Harbor"
        idYacht:
          type: number
          description: "Id Yacht"
        price1W:
          type: number
          description: "Price 1 W"
        price2W:
          type: number
          description: "Price 2 W"
        price3W:
          type: number
          description: "Price 3 W"
    NccruiseList:
      type: object
      properties:
        data:
          type: array
          items: {$ref: "#/components/schemas/Nccruise"}
        total: {type: integer}
        page: {type: integer}
        limit: {type: integer}
    ValidationError:
      type: object
      properties:
        errors:
          type: object
          additionalProperties:
            type: array
            items: {type: string}
    Ncyacht:
      type: object
      properties:
        id:
          type: integer
          readOnly: true
        yachtName:
          type: string
          description: "Yacht Name"
        yachtType:
          type: string
          description: "Yacht Type"
        length:
          type: number
          description: "Length"
        width:
          type: number
          description: "Width"
        draft:
          type: number
          description: "Draft"
        sailSurface:
          type: number
          description: "Sail Surface"
        motor:
          type: number
          description: "Motor"
        headRoom:
          type: number
          description: "Head Room"
        bunks:
          type: number
          description: "Bunks"
        createdAt:
          type: string
          format: date-time
          readOnly: true
        updatedAt:
          type: string
          format: date-time
          readOnly: true
    NcyachtInput:
      type: object
      required: ["yachtName", "yachtType", "length", "width", "draft", "sailSurface", "motor", "headRoom", "bunks"]
      properties:
        yachtName:
          type: string
          description: "Yacht Name"
        yachtType:
          type: string
          description: "Yacht Type"
        length:
          type: number
          description: "Length"
        width:
          type: number
          description: "Width"
        draft:
          type: number
          description: "Draft"
        sailSurface:
          type: number
          description: "Sail Surface"
        motor:
          type: number
          description: "Motor"
        headRoom:
          type: number
          description: "Head Room"
        bunks:
          type: number
          description: "Bunks"
    NcyachtList:
      type: object
      properties:
        data:
          type: array
          items: {$ref: "#/components/schemas/Ncyacht"}
        total: {type: integer}
        page: {type: integer}
        limit: {type: integer}

x-business-rules:
  - id: NCCRUISE_NSD_BR-001
    description: "Conditional"
    severity: WARN
    condition: "*DATA > 0"
  - id: NCCRUISE_NSD_BR-002
    description: "Error handler"
    severity: WARN
    condition: ""
  - id: NCCRUISE_NSD_BR-003
    description: "DECIDE ON *PF-KEY"
    severity: WARN
    condition: "*PF-KEY"
    affectedFields:
      - *PF-KEY
  - id: NCCRUISE_NSD_BR-004
    description: "Conditional"
    severity: WARN
    condition: "#CR-ID-FIND = 0"
  - id: NCCRUISE_NSD_BR-001
    description: "DECIDE ON *PF-KEY"
    severity: WARN
    condition: "*PF-KEY"
    affectedFields:
      - *PF-KEY
  - id: NCCRUISE_NSD_BR-002
    description: "DECIDE ON FIRST"
    severity: WARN
    condition: "FIRST"
    affectedFields:
      - FIRST
  - id: NCCRUISE_NSD_BR-001
    description: "Derivation of #REC-COUNT"
    severity: WARN
    condition: "#REC-COUNT + 1"
    affectedFields:
      - #REC-COUNT
  - id: NCCRUISE_NSD_BR-001
    description: "Conditional"
    severity: WARN
    condition: "NO RECORDS FOUND"
  - id: NCCRUISE_NSD_BR-002
    description: "DECIDE ON #CR-STATUS"
    severity: WARN
    condition: "#CR-STATUS"
    affectedFields:
      - #CR-STATUS
