"use client";
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { useRouter } from "next/navigation";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { Switch } from "@/components/ui/switch";
import { sendMailSchema, type SendMailInput } from "@/lib/validation/sendMail";

// Send Mail — create/edit form, generated by RNC from the UIR.
export default function SendMailForm() {
  const router = useRouter();
  const { register, handleSubmit, setValue, formState: { errors } } =
    useForm<SendMailInput>({ resolver: zodResolver(sendMailSchema) });

  async function onSubmit(values: SendMailInput) {
    const res = await fetch("/api/send_mails", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(values),
    });
    if (res.ok) router.push("/send_mails");
  }

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="container mx-auto max-w-xl space-y-4 py-8">
      <h1 className="text-2xl font-bold">Send Mail</h1>
      <Button type="submit">Save</Button>
    </form>
  );
}
