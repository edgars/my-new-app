"use client";
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { useRouter } from "next/navigation";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { Switch } from "@/components/ui/switch";
import { contactDeveloperSchema, type ContactDeveloperInput } from "@/lib/validation/contactDeveloper";

// Contact Developer — create/edit form, generated by RNC from the UIR.
export default function ContactDeveloperForm() {
  const router = useRouter();
  const { register, handleSubmit, setValue, formState: { errors } } =
    useForm<ContactDeveloperInput>({ resolver: zodResolver(contactDeveloperSchema) });

  async function onSubmit(values: ContactDeveloperInput) {
    const res = await fetch("/api/contact_developers", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(values),
    });
    if (res.ok) router.push("/contact_developers");
  }

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="container mx-auto max-w-xl space-y-4 py-8">
      <h1 className="text-2xl font-bold">Contact Developer</h1>
      <Button type="submit">Save</Button>
    </form>
  );
}
