"use client";
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { useRouter } from "next/navigation";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { Switch } from "@/components/ui/switch";
import { databaseBackupSchema, type DatabaseBackupInput } from "@/lib/validation/databaseBackup";

// Database Backup — create/edit form, generated by RNC from the UIR.
export default function DatabaseBackupForm() {
  const router = useRouter();
  const { register, handleSubmit, setValue, formState: { errors } } =
    useForm<DatabaseBackupInput>({ resolver: zodResolver(databaseBackupSchema) });

  async function onSubmit(values: DatabaseBackupInput) {
    const res = await fetch("/api/database_backups", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(values),
    });
    if (res.ok) router.push("/database_backups");
  }

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="container mx-auto max-w-xl space-y-4 py-8">
      <h1 className="text-2xl font-bold">Database Backup</h1>
      <Button type="submit">Save</Button>
    </form>
  );
}
