-- Generated by RNC — sqlite dialect

CREATE TABLE NCCRUISE (
    CRUISE_ID  REAL NOT NULL,
    CRUISE_STATUS  TEXT NOT NULL,
    START_DATE  REAL,
    START_TIME  REAL NOT NULL,
    END_DATE  REAL NOT NULL,
    END_TIME  REAL NOT NULL,
    START_HARBOR  TEXT,
    DESTINATION_HARBOR  TEXT,
    ID_YACHT  REAL NOT NULL,
    PRICE_1W  NUMERIC NOT NULL,
    PRICE_2W  NUMERIC NOT NULL,
    PRICE_3W  NUMERIC NOT NULL,
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL,
    PRIMARY KEY (CRUISE_ID)
);

CREATE TABLE NCYACHT (
    YACHT_ID  REAL NOT NULL,
    YACHT_NAME  TEXT NOT NULL,
    YACHT_TYPE  TEXT NOT NULL,
    LENGTH  NUMERIC NOT NULL,
    WIDTH  NUMERIC NOT NULL,
    DRAFT  NUMERIC NOT NULL,
    SAIL_SURFACE  NUMERIC NOT NULL,
    MOTOR  NUMERIC NOT NULL,
    HEAD_ROOM  NUMERIC NOT NULL,
    BUNKS  NUMERIC NOT NULL,
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL,
    PRIMARY KEY (YACHT_ID)
);


-- UI Settings — per-entity grid column overrides (visibility, label, order).
CREATE TABLE IF NOT EXISTS ui_settings (
  entity     TEXT PRIMARY KEY,
  columns    TEXT NOT NULL,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);
