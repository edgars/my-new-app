-- Generated by RNC — sqlite dialect

CREATE TABLE AUDITORIA (
    NUM_AUDITORIA  REAL,
    DT_EVENTO  REAL,
    HR_EVENTO  REAL,
    TS_EVENTO  REAL,
    COD_ACAO  TEXT,
    COD_MODULO  TEXT,
    DES_ACAO  TEXT,
    TIPO_ENTIDADE  TEXT,
    ID_ENTIDADE  TEXT,
    NUM_CPF_AFETADO  TEXT,
    USR_EVENTO  TEXT,
    NOME_USUARIO  TEXT,
    COD_PERFIL  TEXT,
    COD_LOTACAO  TEXT,
    IP_ORIGEM  TEXT,
    ID_SESSAO  TEXT,
    NUM_CICLO_BATCH  REAL,
    NUM_SEQ_BATCH  REAL,
    NOM_JOB_BATCH  TEXT,
    SIT_BATCH  TEXT,
    DES_ERRO_BATCH  TEXT,
    ID_CORRELACAO  TEXT,
    NUM_SEQ_CORRELACAO  REAL,
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL
);

CREATE TABLE PAGAMENTO (
    NUM_PAGAMENTO  REAL,
    NUM_CPF  TEXT,
    NUM_INSCRICAO  REAL,
    COD_PROGRAMA  TEXT,
    ANO_MES_REF  REAL,
    NUM_CICLO  REAL,
    VLR_BRUTO  REAL,
    VLR_LIQUIDO  REAL,
    VLR_DESCONTO_TOTAL  REAL,
    SIT_PAGAMENTO  TEXT,
    DT_GERACAO  REAL,
    HR_GERACAO  REAL,
    DT_EMISSAO  REAL,
    DT_CONFIRMACAO  REAL,
    DT_CANCELAMENTO  REAL,
    MOT_CANCELAMENTO  TEXT,
    COD_BANCO  TEXT,
    COD_AGENCIA  TEXT,
    NUM_CONTA  TEXT,
    TIPO_CONTA  TEXT,
    COD_OPERACAO  TEXT,
    NUM_OB_SIAFI  TEXT,
    NUM_NE_SIAFI  TEXT,
    COD_UG_EMITENTE  TEXT,
    COD_GESTAO  TEXT,
    SIT_INTEG_SIAFI  TEXT,
    DT_CONCILIACAO  REAL,
    SIT_CONCILIACAO  TEXT,
    VLR_CONCILIADO  REAL,
    COD_RETORNO_BANCO  TEXT,
    DES_RETORNO_BANCO  TEXT,
    HASH_ARQ_REMESSA  TEXT,
    HASH_ARQ_RETORNO  TEXT,
    DT_INCLUSAO  REAL,
    HR_INCLUSAO  REAL,
    USR_INCLUSAO  TEXT,
    DT_ULT_ALTERACAO  REAL,
    HR_ULT_ALTERACAO  REAL,
    USR_ULT_ALTERACAO  TEXT,
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL
);

CREATE TABLE PAGAMENTO_GRP_DESCONTO (
    OCCURRENCE  INTEGER NOT NULL,
    TIPO_DESCONTO  TEXT,
    VLR_DESCONTO  REAL,
    PCT_DESCONTO  REAL,
    NUM_PROCESSO  TEXT,
    DT_INICIO_DSCT  REAL,
    DT_FIM_DSCT  REAL,
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL,
    PRIMARY KEY (OCCURRENCE)
);

CREATE TABLE PROGRAMA_SOCIAL (
    COD_PROGRAMA  TEXT,
    NOME_PROGRAMA  TEXT,
    SIGLA_PROGRAMA  TEXT,
    TIPO_PROGRAMA  TEXT,
    ORGAO_RESPONSAVEL  TEXT,
    LEI_CRIACAO  TEXT,
    DT_CRIACAO  REAL,
    DT_ENCERRAMENTO  REAL,
    SIT_PROGRAMA  TEXT,
    VLR_BASE_INDIVIDUAL  REAL,
    VLR_BASE_FAMILIAR  REAL,
    VLR_TETO_BENEF  REAL,
    VLR_PISO_BENEF  REAL,
    PCT_REAJUSTE_ANUAL  REAL,
    DT_ULT_REAJUSTE  REAL,
    FATOR_K  REAL,
    RENDA_MAX_PERCAP  REAL,
    IDADE_MIN  REAL,
    IDADE_MAX  REAL,
    IND_EXIGE_FILHOS  TEXT,
    QTD_MIN_FILHOS  REAL,
    IND_EXIGE_ESCOLA  TEXT,
    IND_EXIGE_VACINA  TEXT,
    IND_EXIGE_PRENATAL  TEXT,
    IND_EXIGE_BIOMETRIA  TEXT,
    DT_INCLUSAO  REAL,
    USR_INCLUSAO  TEXT,
    DT_ULT_ALTERACAO  REAL,
    USR_ULT_ALTERACAO  TEXT,
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL
);

CREATE TABLE PROGRAMA_SOCIAL_GRP_FAIXA_CALCULO (
    OCCURRENCE  INTEGER NOT NULL,
    RENDA_INICIO  REAL,
    RENDA_FIM  REAL,
    FATOR_MULTIPLICADOR  REAL,
    VLR_ADICIONAL  REAL,
    IND_ACUMULATIVO  TEXT,
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL,
    PRIMARY KEY (OCCURRENCE)
);

CREATE TABLE PROGRAMA_SOCIAL_GRP_PARAM_REGIONAL (
    OCCURRENCE  INTEGER NOT NULL,
    COD_REGIAO  TEXT,
    FATOR_REGIONAL  REAL,
    VLR_COMPLEMENTO_REG  REAL,
    IND_ATIVO_REGIAO  TEXT,
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL,
    PRIMARY KEY (OCCURRENCE)
);

CREATE TABLE BENEFICIARIO (
    NUM_INSCRICAO  REAL,
    NUM_CPF  TEXT,
    NOME_COMPLETO  TEXT,
    NOME_MAE  TEXT,
    NOME_PAI  TEXT,
    DT_NASCIMENTO  REAL,
    SEXO  TEXT,
    EST_CIVIL  TEXT,
    RG_NUMERO  TEXT,
    RG_ORGAO  TEXT,
    RG_UF  TEXT,
    RG_DT_EXPEDICAO  REAL,
    LOGRADOURO  TEXT,
    NUMERO  TEXT,
    COMPLEMENTO  TEXT,
    BAIRRO  TEXT,
    MUNICIPIO  TEXT,
    UF  TEXT,
    CEP  REAL,
    COD_IBGE  REAL,
    COD_REGIAO  TEXT,
    COD_PROGRAMA  TEXT,
    DT_CADASTRO  REAL,
    DT_INICIO_BENEF  REAL,
    DT_FIM_BENEF  REAL,
    SIT_BENEFICIARIO  TEXT,
    MOT_SITUACAO  TEXT,
    DT_ULT_SITUACAO  REAL,
    VLR_RENDA_FAMILIAR  REAL,
    QTD_MEMBROS_FAMILIA  REAL,
    IND_RENDA_PERCAP  REAL,
    TEL_FIXO  TEXT,
    TEL_CELULAR  TEXT,
    EMAIL  TEXT,
    IND_BIOMETRIA  TEXT,
    DT_COLETA_BIO  REAL,
    COD_POSTO_BIO  TEXT,
    HASH_DIGITAL  TEXT,
    DT_INCLUSAO  REAL,
    HR_INCLUSAO  REAL,
    USR_INCLUSAO  TEXT,
    DT_ULT_ALTERACAO  REAL,
    HR_ULT_ALTERACAO  REAL,
    USR_ULT_ALTERACAO  TEXT,
    NUM_VERSAO  REAL,
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL
);

CREATE TABLE BENEFICIARIO_GRP_DEPENDENTE (
    OCCURRENCE  INTEGER NOT NULL,
    CPF_DEPENDENTE  TEXT,
    NOME_DEPENDENTE  TEXT,
    DT_NASC_DEPEND  REAL,
    PARENTESCO  TEXT,
    SIT_DEPENDENTE  TEXT,
    IND_DEFICIENCIA  TEXT,
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL,
    PRIMARY KEY (OCCURRENCE)
);


-- UI Settings — per-entity grid column overrides (visibility, label, order).
CREATE TABLE IF NOT EXISTS ui_settings (
  entity     TEXT PRIMARY KEY,
  columns    TEXT NOT NULL,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);
