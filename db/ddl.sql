-- Generated by RNC — sqlite dialect

CREATE TABLE CURSO (
    ID  INTEGER NOT NULL,
    ABVCURSO  TEXT NOT NULL,
    NOMECURSO  TEXT NOT NULL,
    ATIVO  TEXT NOT NULL,
    INTERESSECORPORACAO  TEXT NOT NULL,
    DATA_ULT_ALTERACAO  TEXT NOT NULL,
    USUARIO_ULT_ALTERACAO  TEXT NOT NULL,
    VERSAO  INTEGER NOT NULL,
    ABV_CURSO  TEXT NOT NULL,
    INTERESSE_CORPORACAO  TEXT NOT NULL,
    NOME_CURSO  TEXT NOT NULL,
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL,
    PRIMARY KEY (ID)
);

CREATE TABLE FUNCIONARIO (
    ID  INTEGER NOT NULL,
    NOME  TEXT NOT NULL,
    NOMEGUERRA  TEXT,
    NUMEROFUNCIONAL  TEXT NOT NULL,
    CPF  TEXT,
    SEXO  TEXT,
    FONE  TEXT,
    VINCULOVALIDO  INTEGER,
    CELULAR  TEXT,
    EMAIL  TEXT,
    ESTADOCIVIL  TEXT,
    GRUPOSANGUINEO  TEXT,
    RACACOR  TEXT,
    DATANASCIMENTO  TEXT,
    CIDADENASCIMENTO  TEXT,
    UFNASCIMENTO  TEXT,
    PAISNASCIMENTO  TEXT,
    NACIONALIDADE  TEXT,
    NOMEPAI  TEXT,
    NOMEMAE  TEXT,
    PRONTUARIOHPM  TEXT,
    TIPOCADASTRO  TEXT,
    ATIVO  TEXT,
    DOCUMENTOSFUNCIONARIO  TEXT,
    DADOSBANCARIOSFUNCIONARIO  TEXT,
    HISTORICOPROFISSIONAL  TEXT,
    VESTIMENTA  TEXT,
    TAMANHO  TEXT,
    ADIDO  TEXT,
    COROLHOS  TEXT,
    CORCABELOS  TEXT,
    SINAISPARTICULARES  TEXT,
    ALTURA  TEXT,
    DATA_ULT_ALTERACAO  TEXT NOT NULL,
    USUARIO_ULT_ALTERACAO  TEXT NOT NULL,
    VERSAO  INTEGER NOT NULL,
    CIDADE_NASCIMENTO  TEXT,
    AGENCIA_CONTA_BANCO  TEXT,
    BANCO_CONTA_BANCO  TEXT,
    BANCO_PIS_PASEP  TEXT,
    CONTA_CONTA_BANCO  TEXT,
    DATA_PIS_PASEP  TEXT,
    GERA_PASEP  TEXT,
    NUMERO_PIS_PASEP  TEXT,
    TIPO_PAGAMENTO_CONTA_BANCO  TEXT,
    DATA_NASCIMENTO  TEXT,
    CATEGORIACNH  TEXT,
    CATEGORIA_DOCUMENTOM  TEXT,
    DATACTPSIDENTIDADEP  TEXT,
    DATA_CARTEIRA_PROFISSIONAL  TEXT,
    DATA_EMISSAOCNH  TEXT,
    DATA_EXPEDICAO_DOCUMENTOM  TEXT,
    DATA_EXPEDICAORG  TEXT,
    NUMEROCNH  TEXT,
    NUMERO_CARTEIRA_PROFISSIONAL  TEXT,
    NUMERO_DOCUMENTOM  TEXT,
    NUMERO_IDENTIDADE_PROFISSIONAL  TEXT,
    NUMERORG  TEXT,
    NUMERO_TITULO_ELEITOR  TEXT,
    ORGAO_DOCUMENTOM  TEXT,
    ORGAO_EXPEDITORRG  TEXT,
    SECAO_TITULO_ELEITOR  TEXT,
    SERIE_CARTEIRA_PROFISSIONAL  TEXT,
    SERIE_DOCUMENTOM  TEXT,
    SERIE_IDENTIDADE_PROFISSIONAL  TEXT,
    TIPO_IDENTIDADE_PROFISSIONAL  TEXT,
    TIPORG  TEXT,
    UFCNH  TEXT,
    UF_CARTEIRA_PROFISSIONAL  TEXT,
    UF_DOCUMENTOM  TEXT,
    UF_IDENTIDADE_PROFISSIONAL  TEXT,
    UFRG  TEXT,
    UF_TITULO_ELEITOR  TEXT,
    VALIDADECNH  TEXT,
    ZONA_TITULO_ELEITOR  TEXT,
    ESTADO_CIVIL  TEXT,
    GRUPO_SANGUINEO  TEXT,
    AGREGADO  TEXT,
    ANO_PRIMEIRO_EMPREGO  TEXT,
    ANOS_ADICIONAL  TEXT,
    ANOS_AVERBADO  TEXT,
    ANOS_DESCONTO  TEXT,
    APTO_JUNTA_MEDICA  TEXT,
    ARQUIVO  TEXT,
    BOLETIM_COMPORTAMENTO  TEXT,
    BOLETIM_INCORPORACAO  TEXT,
    CIVIL  TEXT,
    COMPORTAMENTO  TEXT,
    DATAATDP  TEXT,
    DATA_APTO_JUNTA_MEDICA  TEXT,
    DATACPP  TEXT,
    DATA_CENSO  TEXT,
    DATA_INATIVIDADE  TEXT,
    DATA_INCORPORACAO  TEXT,
    DATA_REFORMA  TEXT,
    DATA_SUBSIDIO  TEXT,
    DIAS_ADICIONAL  TEXT,
    DIAS_AVERBADO  TEXT,
    DIAS_DESCONTO  TEXT,
    ESCOLARIDADE  TEXT,
    FEZCPP  TEXT,
    FORMA_REMUNERACAO  TEXT,
    LOCAL_AGREGADO  TEXT,
    MATRICULA  TEXT,
    MESES_ADICIONAL  TEXT,
    MESES_AVERBADO  TEXT,
    MESES_DESCONTO  TEXT,
    MOTIVO_INATIVIDADE  TEXT,
    NUMERO_ALMANAQUE  INTEGER,
    OBSCPP  TEXT,
    REFERENCIA  TEXT,
    SUB_JUDICE  TEXT,
    UF_EMPREGO_ANTERIOR  TEXT,
    VALORATDP  INTEGER,
    NOME_GUERRA  TEXT,
    NOME_MAE  TEXT,
    NOME_PAI  TEXT,
    NUMERO_FUNCIONAL  TEXT NOT NULL,
    PAIS_NASCIMENTO  TEXT,
    RACA_COR  TEXT,
    TIPO_CADASTRO  TEXT,
    UF_NASCIMENTO  TEXT,
    POSTO_GRADUACAO  INTEGER,
    ID_FOTO  INTEGER,
    LOTACAO_ATUAL  INTEGER,
    VINCULO_VALIDO  INTEGER,
    TIPO_FUNCIONARIO  INTEGER,
    DATA_COMPORTAMENTO  TEXT,
    LOTACAO_ATUALQO  INTEGER,
    LOCAL_ADIDO  INTEGER,
    COR_CABELOS  TEXT,
    COR_OLHOS  TEXT,
    DATA_AFASTAMENTO  TEXT,
    MOTIVO_AFASTAMENTO  TEXT,
    SINAIS_PARTICULARES  TEXT,
    TEMPO_SERVICO  TEXT,
    ID_ASSINATURA  INTEGER,
    ID_IMPRESSAO_DIGITAL  INTEGER,
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL,
    PRIMARY KEY (ID)
);

CREATE TABLE LOTACAO (
    FUNCIONARIOAUXLOOKUP  TEXT,
    ID  INTEGER NOT NULL,
    DATAINICIO  TEXT,
    DATAFIM  TEXT,
    ATUAL  TEXT,
    SUBSTITUICAO  TEXT,
    ATIVO  TEXT,
    OBS  TEXT,
    DATA_ULT_ALTERACAO  TEXT NOT NULL,
    USUARIO_ULT_ALTERACAO  TEXT NOT NULL,
    VERSAO  INTEGER NOT NULL,
    DATA_FIM  TEXT,
    DATA_INICIO  TEXT,
    FUNCAOQDI  INTEGER NOT NULL,
    FUNCIONARIO  INTEGER,
    LOCAL  INTEGER NOT NULL,
    POSTO_GRADUACAO  INTEGER,
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL,
    PRIMARY KEY (ID)
);

CREATE TABLE MESANOFERIAS (
    ID  INTEGER NOT NULL,
    MES  TEXT NOT NULL,
    ANO  INTEGER NOT NULL,
    FUNCIONARIO  TEXT,
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL,
    PRIMARY KEY (ID)
);


-- UI Settings — per-entity grid column overrides (visibility, label, order).
CREATE TABLE IF NOT EXISTS ui_settings (
  entity     TEXT PRIMARY KEY,
  columns    TEXT NOT NULL,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);
