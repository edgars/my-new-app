-- Generated by RNC — sqlite dialect

CREATE TABLE PARTS (
    PARTNO  REAL NOT NULL,
    DESCRIPTION  TEXT NOT NULL,
    ONHAND  REAL,
    ONORDER  REAL,
    VENDORNO  REAL,
    COST  NUMERIC,
    LISTPRICE  NUMERIC,
    BACKORD  INTEGER,
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL
);

CREATE TABLE VENDORS (
    VENDORNO  INTEGER NOT NULL,
    VENDORNAME  TEXT NOT NULL,
    CONTACTEMAIL  TEXT,
    ACTIVESTATUS  INTEGER,
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL,
    PRIMARY KEY (VENDORNO)
);

CREATE TABLE ORDERS (
    ORDERNO  REAL,
    CUSTNO  REAL NOT NULL,
    SALEDATE  TEXT,
    SHIPDATE  TEXT,
    SHIPTOCONTACT  TEXT,
    SHIPTOADDR1  TEXT,
    SHIPTOADDR2  TEXT,
    SHIPTOCITY  TEXT,
    SHIPTOSTATE  TEXT,
    SHIPTOZIP  TEXT,
    SHIPTOCOUNTRY  TEXT,
    SHIPTOPHONE  TEXT,
    SHIPVIA  TEXT,
    PO  TEXT,
    EMPNO  INTEGER NOT NULL,
    TERMS  TEXT,
    PAYMENTMETHOD  TEXT,
    ITEMSTOTAL  NUMERIC,
    TAXRATE  REAL,
    TAXTOTAL  NUMERIC,
    FREIGHT  NUMERIC,
    AMOUNTPAID  NUMERIC,
    AMOUNTDUE  NUMERIC,
    SALESPERSON  TEXT,
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL
);

CREATE TABLE CUSTOMER (
    CUSTNO  REAL,
    COMPANY  TEXT,
    ADDR1  TEXT,
    ADDR2  TEXT,
    CITY  TEXT,
    STATE  TEXT,
    ZIP  TEXT,
    COUNTRY  TEXT,
    PHONE  TEXT,
    FAX  TEXT,
    TAXRATE  REAL,
    CONTACT  TEXT,
    LASTINVOICEDATE  TEXT,
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL
);

CREATE TABLE EMPLOYEE (
    EMPNO  INTEGER,
    FULLNAME  TEXT,
    LASTNAME  TEXT,
    FIRSTNAME  TEXT,
    PHONEEXT  TEXT,
    HIREDATE  TEXT,
    SALARY  REAL,
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL
);


-- UI Settings — per-entity grid column overrides (visibility, label, order).
CREATE TABLE IF NOT EXISTS ui_settings (
  entity     TEXT PRIMARY KEY,
  columns    TEXT NOT NULL,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);
