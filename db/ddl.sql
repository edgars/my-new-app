-- Generated by RNC — sqlite dialect

CREATE TABLE FUNCIONARIO (
    ID  INTEGER NOT NULL,
    NOME  TEXT NOT NULL,
    NOMEGUERRA  TEXT,
    NUMEROFUNCIONAL  TEXT NOT NULL,
    CPF  TEXT,
    SEXO  TEXT,
    FONE  TEXT,
    VINCULOVALIDO  INTEGER,
    CELULAR  TEXT,
    EMAIL  TEXT,
    ESTADOCIVIL  TEXT,
    GRUPOSANGUINEO  TEXT,
    RACACOR  TEXT,
    DATANASCIMENTO  TEXT,
    CIDADENASCIMENTO  TEXT,
    UFNASCIMENTO  TEXT,
    PAISNASCIMENTO  TEXT,
    NACIONALIDADE  TEXT,
    NOMEPAI  TEXT,
    NOMEMAE  TEXT,
    PRONTUARIOHPM  TEXT,
    TIPOCADASTRO  TEXT,
    ATIVO  TEXT,
    DOCUMENTOSFUNCIONARIO  TEXT,
    DADOSBANCARIOSFUNCIONARIO  TEXT,
    HISTORICOPROFISSIONAL  TEXT,
    VESTIMENTA  TEXT,
    TAMANHO  TEXT,
    ADIDO  TEXT,
    COROLHOS  TEXT,
    CORCABELOS  TEXT,
    SINAISPARTICULARES  TEXT,
    ALTURA  TEXT,
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL,
    PRIMARY KEY (ID)
);

CREATE TABLE DEPENDENTE (
    ID  INTEGER NOT NULL,
    NOME  TEXT NOT NULL,
    DATANASCIMENTO  TEXT,
    PARENTESCO  TEXT,
    CPF  TEXT,
    SEXO  TEXT,
    DADOSBANCARIOS  TEXT,
    DOCUMENTOS  TEXT,
    ENDERECO  TEXT,
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL,
    PRIMARY KEY (ID)
);

CREATE TABLE LOTACAO (
    FUNCIONARIOAUXLOOKUP  TEXT,
    ID  INTEGER NOT NULL,
    DATAINICIO  TEXT,
    DATAFIM  TEXT,
    ATUAL  TEXT,
    SUBSTITUICAO  TEXT,
    ATIVO  TEXT,
    OBS  TEXT,
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL,
    PRIMARY KEY (ID)
);

CREATE TABLE AVISOSUSUARIO (
    ID  INTEGER NOT NULL,
    MENSAGEM  TEXT NOT NULL,
    PERFIL  TEXT,
    DATA  TEXT NOT NULL,
    ITEMIS  INTEGER,
    VALIDADO  TEXT,
    IGNORAR  TEXT,
    LANCADO  TEXT,
    IGNORARCHEFIA  TEXT,
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL,
    PRIMARY KEY (ID)
);


-- UI Settings — per-entity grid column overrides (visibility, label, order).
CREATE TABLE IF NOT EXISTS ui_settings (
  entity     TEXT PRIMARY KEY,
  columns    TEXT NOT NULL,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);
