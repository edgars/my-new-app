// Sidebar navigation — generated by RNC from the UIR.
// `icon` is a statically-imported Lucide component (no lucide-react/dynamic) so only
// the icons actually used are bundled — keeps dev compile fast.
import { Briefcase, Table, type LucideIcon } from 'lucide-react';

export interface NavItem { label: string; href: string; icon: LucideIcon }

export const appName = "safo";

export const navItems: NavItem[] = [
  { label: "Funcionarios", href: "/funcionarios", icon: Briefcase },
  { label: "Auditoria Detalhada Funcionarios", href: "/auditoria_detalhada_funcionarios", icon: Briefcase },
  { label: "Funcao Qdis", href: "/funcao_qdis", icon: Table },
  { label: "Tipo Funcionarios", href: "/tipo_funcionarios", icon: Briefcase },
];
