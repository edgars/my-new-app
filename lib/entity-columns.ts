// Default grid columns — generated by RNC from the UIR. Seeds which fields start
// visible in the table; full field config (order + form/grid visibility) lives in
// lib/ui-settings.ts. Edit via /settings.
type GridColDefault = { key: string; defaultLabel: string; label: string; visible: boolean; order: number; format?: "currency" | "date" | "boolean" };

export const ENTITY_COLS: Record<string, GridColDefault[]> = {
  nccruises: [
    { key: "cruiseId", defaultLabel: "Cruise Id", label: "Cruise Id", visible: true, order: 0 },
    { key: "cruiseStatus", defaultLabel: "Cruise Status", label: "Cruise Status", visible: true, order: 1 },
    { key: "startDate", defaultLabel: "Start Date", label: "Start Date", visible: true, order: 2 },
    { key: "startTime", defaultLabel: "Start Time", label: "Start Time", visible: true, order: 3 },
    { key: "endDate", defaultLabel: "End Date", label: "End Date", visible: true, order: 4 },
  ],
  ncyachts: [
    { key: "yachtName", defaultLabel: "Yacht Name", label: "Yacht Name", visible: true, order: 0 },
    { key: "yachtId", defaultLabel: "Yacht Id", label: "Yacht Id", visible: true, order: 1 },
    { key: "yachtType", defaultLabel: "Yacht Type", label: "Yacht Type", visible: true, order: 2 },
    { key: "length", defaultLabel: "Length", label: "Length", visible: true, order: 3, format: "currency" as const },
    { key: "width", defaultLabel: "Width", label: "Width", visible: true, order: 4, format: "currency" as const },
  ],
};

export const ENTITY_LABELS: Record<string, string> = {
  nccruises: "Nccruises",
  ncyachts: "Ncyachts",
};
